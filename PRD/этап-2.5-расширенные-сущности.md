# PRD Этап 2.5: Расширенные MCP-инструменты

## Цель
Реализовать оставшиеся сущности Vetmanager API как MCP-инструменты.
Каждый файл ≤ 150 строк, паттерн идентичен существующим tools/*.py.

## Группы

### 2.5.1 Справочные
- `Breed` → `/rest/api/breed` — GET list, GET by id
- `PetType` → `/rest/api/petType` — GET list, GET by id
- `City` → `/rest/api/city` — GET list, GET by id
- `CityType` → `/rest/api/cityType` — GET list
- `Street` → `/rest/api/street` — GET list, GET by id
- `Unit` → `/rest/api/unit` — GET list, GET by id
- `Role` → `/rest/api/role` — GET list, GET by id
- `UserPosition` → `/rest/api/userPosition` — GET list, GET by id
- `ComboManualName` → `/rest/api/ComboManualName` — GET list, GET by id
- `ComboManualItem` → `/rest/api/ComboManualItem` — GET list, GET by id

### 2.5.2 Финансовые
- `Payment` → `/rest/api/payment` — GET list, GET by id, POST
- `ClosingOfInvoices` → `/rest/api/closingOfInvoices` — GET list, GET by id
- `InvoiceDocument` → `/rest/api/invoiceDocument` — GET list, GET by id, POST
- `Cassa` → `/rest/api/cassa` — GET list, GET by id
- `CassaClose` → `/rest/api/cassaclose` — GET list, GET by id

### 2.5.3 Складские
- `GoodGroup` → `/rest/api/GoodGroup` — GET list, GET by id
- `GoodSaleParam` → `/rest/api/goodSaleParam` — GET list, GET by id
- `PartyAccount` → `/rest/api/PartyAccount` — GET list, GET by id
- `PartyAccountDoc` → `/rest/api/PartyAccountDoc` — GET list, GET by id
- `StoreDocument` → `/rest/api/StoreDocument` — GET list, GET by id
- `Suppliers` → `/rest/api/Suppliers` — GET list, GET by id

### 2.5.4 Клинические
- `Hospital` → `/rest/api/hospital` — GET list, GET by id, POST
- `HospitalBlock` → `/rest/api/HospitalBlock` — GET list, GET by id
- `Diagnoses` → `/rest/api/MedicalCards/AllDiagnoses` — GET list

### 2.5.5 Операционные
- `Clinics` → `/rest/api/clinics` — GET list, GET by id
- `Timesheet` → `/rest/api/timesheet` — GET list, GET by id
- `Properties` → `/rest/api/properties` — GET list
- `AnonymousClient` → `/rest/api/user/anonymousList` — GET list

## Критерии готовности
- Все файлы созданы в `tools/`
- Зарегистрированы в `tools/__init__.py`
- Образ пересобран, тесты проходят
