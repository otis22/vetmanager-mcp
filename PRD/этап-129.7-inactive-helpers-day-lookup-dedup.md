# Этап 129.7. Inactive helpers day-lookup dedup

## Контекст

После stages `125`, `129.1` и `129.4` в `tools/_inactive_helpers.py` остаётся дублирование:
invoice/medcard day-bounded batched lookup собираются почти одинаково в двух разных функциях.

## Цель

Вынести generic helper для day-window batched entity lookup и перевести invoice/medcard paths на него.

## Scope

1. Добавить один generic helper с параметрами endpoint/id/date/entity keys.
2. Перевести single-owner и multi-client inactive lookup на этот helper.
3. Убедиться, что существующие regression tests остаются зелёными.
