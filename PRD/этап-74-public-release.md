# PRD: Этап 74 — Подготовка к публичному релизу репозитория

## Цель

Сделать репозиторий `otis22/vetmanager-mcp` публичным на GitHub. На лендинге разместить ссылку на репозиторий с примечанием, что любой желающий может развернуть сервер у себя. Причесать README и документацию для внешнего читателя.

## Контекст

- Репозиторий сейчас **private**
- Аудит безопасности пройден: секретов в коде нет, `.gitignore` корректен
- Все credentials передаются через env vars
- Лендинг, web UI, bearer auth, 101 MCP tool — production-ready

## Персоны

- **Внешний разработчик** — хочет развернуть MCP-сервер у себя для своей клиники
- **Ветврач / администратор** — видит на лендинге, что проект open-source, доверяет больше
- **Контрибьютор** — хочет понять как поучаствовать

## Декомпозиция

### 74.1 LICENSE (≤30 мин, ≤10 строк)

Создать файл `LICENSE` с лицензией MIT.

### 74.2 SECURITY.md (≤30 мин, ≤30 строк)

Создать файл `SECURITY.md`:
- Куда сообщать об уязвимостях (email или GitHub Security Advisories)
- Что считается уязвимостью
- Ожидаемое время ответа

### 74.3 README.md (≤2 ч, ≤100 строк diff)

Правки в существующем `README.md`:
- Добавить badges в шапку: CI status, license
- Обезличить примеры: `342915.simplecloud.ru` → `<your-domain>`, `212.193.59.219` → `<your-server-ip>`
- Добавить секцию **Self-hosted** — "проект open-source, можно развернуть у себя"
- Добавить секцию **Contributing** — ссылка на issues
- Добавить **English note** — краткое пояснение для англоязычной аудитории
- НЕ трогать deploy-скрипты (там хардкод домена — это дефолты)

### 74.4 landing_page.py + тесты (≤2 ч, ≤150 строк)

Изменения в `landing_page.py`:
- Topbar nav: добавить ссылку "GitHub" с SVG-иконкой перед "Войти"
- Footer nav: добавить ссылку "GitHub"
- Новая секция "Open Source" перед CTA "Готовы начать?":
  - Текст: проект с открытым кодом, можно развернуть на своём сервере
  - Кнопка-ссылка на GitHub repo

Обновить `tests/test_landing_page.py`:
- Проверить наличие ссылки на GitHub в HTML
- Проверить наличие текста про open source / self-hosted

### 74.5 Публикация репозитория (≤15 мин)

- `gh repo edit otis22/vetmanager-mcp --visibility public`
- Обновить description: "MCP server for Vetmanager veterinary clinic management"
- Добавить topics: `mcp`, `vetmanager`, `ai`, `veterinary`, `model-context-protocol`

### 74.6 AssumptionLog (≤15 мин)

Зафиксировать:
- Выбор MIT лицензии
- Решение обезличить домен в README, но оставить в deploy-скриптах
- Решение не переводить README целиком на английский

## Ограничения

- Не переводим README на английский (слишком большой объём)
- Не трогаем deploy-скрипты
- Не создаём отдельный CONTRIBUTING.md
- Не трогаем artifacts/

## Верификация

- `docker compose run --rm test` — все тесты проходят
- `gh repo view --json visibility` → `PUBLIC`
- README читается внешним разработчиком без знания инфраструктуры
- Лендинг содержит ссылку на GitHub и информацию о self-hosted
