# Roadmap

Статусы: `todo` | `in_progress` | `supervisor_pending` | `done` | `stop`.
Структуру проверяет `scripts/check_roadmap_structure.py`.

## Этап 262. Выгрузки метрик всё ещё склеивают каналы вручную — `todo`

Источник: этап 260. Внутри сервиса активность считается по журналу, но
скрипты отчётности в LiveHelperAgent (`weekly_metrics.sql`,
`stalled_accounts.sql`) до сих пор складывают bearer-логи с отметкой
`last_used_at` на OAuth-токене — иначе на исторических данных ряд обрывается.
Как только журнал накопит собственную историю OAuth-вызовов, склейку нужно
убрать, а разрыв ряда на дате выката 26.08.2026 отметить в отчёте явно.

- 262.1 Убрать склейку из выгрузок и считать по журналу. Сегодня нельзя, и
  условие теперь точное: первая OAuth-запись в журнале — **26.08.2026 21:11
  UTC** (проверено на бою 04.09.2026, 752 записи). Глубина истории OAuth — девять
  дней против месяцев у bearer. Снять склейку можно, когда отчётное окно целиком
  начинается позже 26.08.2026: для окна в 30 дней это **с 25.09.2026**. Раньше
  этой даты выгрузка молча потеряет всю OAuth-активность до выката. — `todo`
- 262.2 Отметить в дашборде дату, до которой OAuth-вызовов в данных нет.
  Сделано 04.09.2026: в блоке каналов сказано, что ряд OAuth начинается 26.08 и
  что сравнивать окно длиннее девяти дней с bearer нельзя. Побочно починена пара
  артефактов: блок каналов собирался в HTML мимо общей функции, и его пояснение
  в парный Markdown не попадало вовсе — витрина знала про начало ряда, источник
  нет. — `done`

## Этап 341. После выпуска ключа человек остаётся один на один с конфигом — `in_progress`

Источник: когортный анализ активации 23.09.2026 (`LiveHelperAgent/workspace/2026-09-23/mcp-cohort-activation.md`)
и таблица `activation_events` прода.

Из 33 живых аккаунтов 26 выпустили ключ и только 21 сделал первый запрос. Пятеро
(#8, #31, #36, #41, #53) остались с ключом и **нулём событий авторизации любого
рода** — ни успеха, ни отказа, ни rate-limit: попытка до сервера не доходит. При
этом `token_copied` есть у всех пятерых, то есть последнее, что мы про них знаем, —
человек скопировал ключ и ушёл в свой клиент. У #53 за три часа 22.09 набралось
14 событий (`integration_saved` пять раз подряд в режиме `user_token`, копирования
token/config/mcp_url) и ни одного входа — это не «передумал», это проигранная
борьба с настройкой.

Что человек видит в этот момент сейчас (`web_html.py`, состояние
`needs_client_use`): «Токен готов; вставьте MCP URL и Authorization bearer token
в клиент» и «Ждём первый запрос от MCP-клиента… страница обновится автоматически».
Технически верно и мотивации не несёт вовсе: не сказано, что осталось мало, не
показано, что человек получит, не предложено, о чём спросить первым.

Материал для примеров берём из пилота bot-panel (`vetclinic-pilot`), а **не** из
профиля вызовов прода: прод-профиль сделан роботами (у аккаунта #7 — 59 107 вызовов
за месяц, три четверти всего трафика — счета), а живой человек без готового
сценария спрашивал другое — расписание врачей и заполнение медкарт. Реальные
формулировки пилота: «Кто из врачей работает сегодня?», «У всех ли принятых
сегодня пациентов заполнены медкарты?», «Кто свободен завтра после 15:00?»,
«Топ-10 должников», «Клиенты без визитов 12+ месяцев».

**Данные в примерах вымышленные** и подписаны как пример: настоящие ответы клиник
показывать нельзя, а пустая витрина «можно спросить X» уже есть и не работает.

Этап **не делает**: не шлёт писем и push-уведомлений, не меняет саму воронку
подключения и тексты OAuth-согласия, не трогает контракт инструментов.

- 341.1 Лендинг, секция `#examples` (`landing_page.py`): вместо голого списка
  вопросов — пары «вопрос → ответ» с правдоподобным вымышленным ответом и подписью
  «пример оформления, цифры вымышленные». Вопросы — из пилота. — `done`
- 341.2 Кабинет, панель `issued-token-panel` сразу после выпуска ключа
  (`web_html.py`): блок «Остался один шаг» — что осталось сделать, **2–3 пары
  «вопрос → ответ» и не больше** (в кабинете человек уже решился, длинная витрина
  ему мешает; шесть пар — только на лендинге), кнопка «скопировать вопрос».
  Данные в ответах те же вымышленные, что на лендинге, с той же подписью. — `done`
- 341.3 Кабинет, состояние `needs_client_use` (`web_html.py`, `waiting_html`):
  заменить сухое ожидание на «осталось немного» — сколько это обычно занимает, что
  страница обновится сама, один вопрос на пробу и явная ссылка «не выходит —
  напишите». — `done`
- 341.4 Подсказки первого запроса (`oauth-first-request-guide`): заменить
  выдуманные фразы на формулировки пилота, те же, что на лендинге. — `done`
- 341.5 Телеметрия, иначе этап не измерить: события «мотиватор показан» и «пример
  скопирован» в `activation_events`; заодно проставлять `auth_mode` у `token_copied`
  — сейчас там всегда `unknown`, и мы не знаем, какой режим человек копировал. — `done`
- 341.6 Строка про оплату зарубежных подписок (spoteeq.ru, ник `@vromanichev24`)
  — **только в кабинете**, рядом с инструкцией подключения клиента
  (`client-connect`). На лендинге не размещать — решение Владимира 23.09.2026:
  публичная страница остаётся про продукт, реклама видна только тому, кто уже
  завёл аккаунт. Одна строка текстом, без баннера. — `done`
- 341.7 Композиция обеих страниц остаётся простой и понятной: мотивация не должна
  превратить лендинг и кабинет в витрину. Правило приёмки — на экране после выпуска
  ключа видно, **что осталось сделать**, без прокрутки и без выбора из многих
  вариантов; блоки мотивации не оттесняют ключ, MCP URL и инструкцию подключения.
  **Визуальное ревью сильными моделями обязательно** и является гейтом этапа: обе
  страницы в светлой и тёмной теме, на десктопе и на ширине телефона, в трёх
  состояниях активации (`needs_token`, `needs_client_use`, готово) —
  `scripts/run_claude_review.sh`, результат через `validate_review_result.py`,
  скриншоты в evidence этапа. Замечания ревью либо закрываются, либо отклоняются
  письменно с причиной. — `in_progress`

## Этап 340. Денежная семантика счёта описана неверно в четырёх полях — `done`

Источник: репорты #89 и #91 от 23.09.2026 (аккаунт 51, оба `human`). Агент
просил рублёвую сумму скидок по проведённым счетам, просуммировал
`invoice.discount` как деньги и отдал пользователю неверный отчёт. Репорт
указал на одно поле; проверка по `vetmanager-extjs` вскрыла весь денежный
блок сущности, и врёт **наш собственный** справочник
`artifacts/api_entity_reference-ru.md`, из которого мы же берём контракт при
разработке:

| поле | справочник | на самом деле | где проверено |
|---|---|---|---|
| `invoice.discount` | «Сумма скидки» | процент скидки | `getDiscountAmount($fullAmount, $percent)` = `fullAmount*$percent/100`, `application/src/Entity/Invoice.php:172` |
| `invoice.increase` | «Флаг повышения цены» | процент наценки | `getIncreaseAmount(...)`, там же `:177` |
| `invoice.amount` | «сумма счета до скидки» | сумма **после** скидки и наценки | `getSumWithPercents`: `sum - discountAmount + increaseAmount`, `:182`, присваивается в `:320` |
| `invoice.percent` | «процент скидки» | нетто-процент `increase - discount` | `getPercent()`, `:519` |
| `invoice_document.price`, `default_price` | не описаны | уже сумма позиции, не цена за единицу | `if(id.quantity = 0, 0, id.default_price / id.quantity) AS price_for_one`, `:583` |

Рублёвая скидка по позиции в штатных отчётах ВМ:
`default_price - price*((100-discount)/100)*((100+increase)/100)`.

Про запись проверено отдельно, потому что первая формулировка была
преувеличена. `update_invoice` зарегистрирован и доступен под
`finance.write`; REST принимает `discount` (правило `numerical` в
`rest/protected/models/Invoice.php:61` делает поле safe, `doRestUpdate`
грузит запись и доваливает payload). Но `beforeSave` REST-модели пересчёта
не делает — меняется только колонка `discount`, а `amount` остаётся прежним.
Значит немедленной порчи итога счёта нет: есть рассинхрон, который вылезет в
штатных отчётах ВМ (они пересчитывают из `id.price` и `i.discount`) и при
следующем сохранении счёта в интерфейсе, где `getSumWithPercents` посчитает
уже по новому проценту. Поэтому описание единиц у записи нужно не меньше,
чем у чтения, но это не аварийный сюжет.

Этап **не** делает: не добавляет инструмент удаления или переименования
сохранённого отчёта (решение по удалению принято ранее и не пересматривается),
не чинит метрику Report AI «Сумма(Скидка)» — это сторона Ветменеджера,
отправлено Даниилу Сироте 23.09 (Битрикс24, сообщение 596271).

- 340.1 Справочник `artifacts/api_entity_reference-ru.md`: исправить четыре
  строки сущности `invoice` и описать `invoice_document.price` /
  `default_price` как суммы позиции. Расхождение занести в
  `artifacts/api-research-notes-ru.md` как lessons learned: контракт денежных
  полей проверяется по коду ВМ, справочник сам по себе не источник истины. — `done`
- 340.2 Описания инструментов: `get_invoices` — единицы `discount`, `increase`,
  `percent`, `amount` и рублёвая формула скидки; `update_invoice` — единицы тех
  же параметров и явная фраза, что запись меняет процент и **не** пересчитывает
  `amount`. Контракт-тесты на присутствие формулировок. — `done`
- 340.3 Report AI helper (`artifacts/report-ai-prompt-helper-short-mcp-2026-06-15.md`):
  абзац про денежную семантику счёта с рублёвой формулой — именно там агент
  строит `intent_text` про скидки, и сейчас про скидку там не сказано ничего. — `done`
- 340.4 Известная проблема по сюжету с плейбуком «`discount` — процент, не
  рубли; рублёвая скидка считается по формуле; не суммируй поле как деньги»,
  привязать #89 и #91. Правила совпадения по `get_invoices` и
  `get_report_ai_prompt_helper`. — `done`

## Этап 336. Отказ провайдера Report AI выглядит для агента как ошибка интента — `done`

Источник: пилот bot-panel 19.09.2026, run `ec8a374d`. Два job подряд (#4 12:41,
#6 12:43 МСК) прошли `queued → recognizing` и через ~60 с упали на стороне
Ветменеджера: `status=failed`, `error_code=PREVIEW_FAILED`,
`error_message_safe="DeepSeek CURL error: Connection timed out after 10000
milliseconds"`. Это транспортный отказ LLM-провайдера (`OpenAiLlmClient` в
vetmanager-extjs: `"{provider} CURL error: {curlError}"`, connect-timeout к
`api.deepseek.com`; фолбэк flash→pro бьёт в тот же хост). MCP отдал `failed`
честно, но без подсказки — агент немедленно пересоздал job с другим
интентом (бесполезно при недоступном провайдере) и `report_problem` не
вызвал: в базе знаний следа нет, пользователь получил «конструктор дважды
вернул таймаут». Единственная существующая аннотация для `PREVIEW_FAILED` —
`report_ai_goods_good_id_preview_failed` — про другой класс отказа.

- 336.1 `mcp_workaround` для `failed` + `PREVIEW_FAILED` с транспортным
  текстом провайдера (маркеры — из кода `OpenAiLlmClient.php` /
  `HttpErrorMapper.php` в `../vetmanager-extjs`, не домысливать: `CURL error`,
  `timed out`, HTTP 5xx/429/408 и т.п.): код `report_ai_provider_unreachable`,
  шаги — интент не переписывать, новый job не создавать сразу, одна повторная
  попытка не раньше чем через несколько минут, иначе прямые инструменты
  (`get_medical_cards_by_date`, `get_admissions` …); `safe_to_retry` с
  задержкой; в шагах — вызвать `report_problem` с кодом отказа. Сторож: тест
  на точный текст 19.09 красный до реализации; отказ с `good.id` и обычный
  `PREVIEW_FAILED` без транспортных маркеров аннотацию не получают. — `done`
- 336.2 Описание `get_report_ai_job` / prompt helper: что означает
  `PREVIEW_FAILED` с транспортным сообщением и что делать (те же шаги, без
  дублирования текста — ссылкой на поле `mcp_workaround`). — `done`

## Этап 337. Пустой поиск сотрудника по полной фамилии оставляет агента без подсказки — `done`

Источник: репорт #84 (19.09.2026, аккаунт 50): `get_users name="Ионова"` вернул
пусто, `name="Ион"` нашёл нужного активного врача. Супервизор снял контракт:
REST `like` в `ERestHelperScopes.php:107` сам оборачивает `%value%`, регистр
не важен; на devtr6 и напрямую через REST, и через `get_users` полная фамилия и
основа дают один и тот же id во всех парах («Астраханцева»/«Астрах»,
«Тихорский»/«Тихор», «Бездоступович»/«Бездоступ»). Значит в карточке врача
фамилия записана иначе (мужская форма, латиница внутри кириллицы, лишние
символы) — дефект данных клиники, не LIKE и не MCP. Триаж: KI-61 из #84
(`workaround_available`, плейбук «повтори по основе → `get_user_by_id`»)
применён супервизором 21.09. Но плейбук доходит только через
`report_problem`; агент, который просто получил пустой список, вывода «врача
нет» не избежит. Авто-фолбэк по основе Владимир отклонил (21.09.2026): он
возвращает «похожих» и это продуктовое решение.

- 337.1 `get_users` при непустом `name` и нулевом результате отдаёт в ответе
  `mcp_hint` (тем же контрактом, что `mcp_workaround`: `code`, `summary`,
  `steps`, `do_not_do`): код `user_name_search_empty`; шаги — повторить с
  основой фамилии 3–4 буквы (параметр ищет и по `first_name`), при нескольких
  совпадениях выбрать по `position_id`/расписанию и подтвердить
  `get_user_by_id`, процитировать фамилию как записана; не делать вывод «врача
  нет / неактивен» после одного пустого точного поиска, не расширять
  `is_active` до `None` вместо основы. Никакого автоматического повторного
  поиска. Непустой результат, поиск без `name`, ошибки транспорта/доступа —
  без подсказки. Сторож: красный до реализации. — `done`
- 337.2 Описание `get_users` в `tool_descriptions.py`: одна фраза про
  подсказку при пустом результате и про поиск по основе, без дублирования
  шагов. — `done`

## Этап 338. Сервер молчит, пока всё хорошо, и это неотличимо от его смерти — `done`

Источник: срезы 17.09 и 21.09.2026 (`logs/mcp/2026-09-{17,21}-product-metrics-snapshot.md`
в LiveHelperAgent). Всё, что мы узнаём о проде, приходит через ручной срез раз в
несколько дней: волну 11 репортов 16.09 увидели через сутки, 60 промахов базы
знаний на экспорте 11.09 — через день, отказ провайдера DeepSeek 19.09 — из
пилота, максимум задержки 82.6 с — постфактум. На сервере стоит только штатный
`/opt/vps-monitor.sh` (крон `*/5`: диск 85 %, RAM 90 %, load 2×ядра, залипший
брейкер) — он молчит, пока порог не пробит, и его молчание неотличимо от его же
смерти или обрыва исходящей связи. Grafana-алертов 0, Prometheus rules 0,
`HC_URL` пустой. Память контейнера (131 → 249 → 270 МБ на трёх процессах) не
имеет ряда: `process_resident_memory_bytes` не экспортируется, cadvisor нет.

Решение (Владимир, 21.09.2026): повторить схему резервного прокси от 17.08 —
суточный heartbeat «жив + показатели» в группу VetmanagerVPS через notify-релей.
Не алертинг, а ежедневный отчёт: пропало сообщение — проблема; цифры дают ряд
без нового экспорта метрик. Второй блок сообщения — сторона Ветменеджера
(апстрим, Report AI); Владимир сам передаёт его Сироте, автоматических
рассылок наружу нет.

Границы: по правилу 15.08.2026 мониторинг — не часть этого репозитория. Скрипт
живёт в LiveHelperAgent (`scripts/deploy/mcp-daily-heartbeat.sh`, рядом с
`daily-heartbeat.sh` прокси), ставится руками на сервер; здесь — только этап
как учёт работы. В `docker-compose.yml`, `ops/`, env и коде сервиса изменений
нет. Секреты релея берутся из уже стоящего `/opt/vps-monitor.sh`, новых не
заводится, в git не попадают.

- 338.1 Скрипт heartbeat. Источники — только то, что уже есть на хосте:
  `uptime`/`free`/`df`/`swapon`; `docker inspect` (health, RestartCount,
  OOMKilled, StartedAt) и `docker stats --no-stream` для контейнера mcp;
  Prometheus с хоста `127.0.0.1:9090` (в контейнере mcp нет curl, метрики за
  токеном — не ходить туда); один `psql` через
  `docker compose exec -T postgres … < /dev/null` (без `< /dev/null` exec
  съедает stdin — грабля 17.09). Блок 1 «наша сторона» за 24 ч: аптайм хоста,
  контейнер (health, МБ/лимит, рестарты, OOM), диск/память/swap/нагрузка,
  `token_auth_succeeded` и число аккаунтов, ошибки инструментов
  (`vetmanager_tool_calls_total{outcome!="success"}`), max/avg задержки
  инструмента, срабатывания и промахи известных проблем
  (`known_issue_lookups_total`), новых репортов в `agent_feedback_reports` с
  номерами. Блок 2 «сторона Ветменеджера» за 24 ч: `upstream_failures_total`
  по reason (5xx, таймауты, 401), средний отклик апстрима,
  `report_ai_job_terminal_outcomes_total` (failed/abandoned_wait/saved),
  `report_ai_exports_total` (start ok/err). Формат релея `{"service","text"}`,
  как в прокси-версии; `--dry-run` печатает сообщение без отправки; код ответа
  релея проверяется, не 200/204 — ненулевой exit. Каждая цифра до первой
  отправки сверяется с `collect.sh`-срезом того же часа, а не принимается на
  веру (обе ошибки первой версии прокси-heartbeat были именно такими).
  Сделано 21.09.2026: скрипт написал кодекс (cwd LiveHelperAgent, без доступа к
  проду), супервизор починил чтение `/opt/vps-monitor.sh` (несколько
  присваиваний на строке через `;` — версия воркера не нашла бы `RELAY_SECRET`)
  и сверил каждую цифру `--dry-run` со срезом того же часа (09:22 UTC): входы
  2332 / 6 аккаунтов, репорт #86, задержка avg 0.17 / max 5.94 с, upstream
  0.12 с, ошибок 0, Report AI 0, серии экспортов нет → `—`. — `done`
- 338.2 Установка на прод: `/usr/local/sbin/mcp-daily-heartbeat.sh` (700, root),
  `/etc/cron.d/mcp-heartbeat` — 07:04 UTC (10:04 МСК, после утреннего брифа).
  Приёмка: `--dry-run` показывает правдоподобные цифры; живая отправка — релей
  ответил 204, сообщение видно в группе VetmanagerVPS. Штатный `vps-monitor.sh`
  не трогать — он остаётся про «сейчас», heartbeat про тренд и тишину.
  Сделано 21.09.2026 09:22 UTC: `/usr/local/sbin/mcp-daily-heartbeat.sh`
  (700 root), `/etc/cron.d/mcp-heartbeat` с логом `/var/log/mcp-heartbeat.log`;
  живая отправка — релей 204. Ошибка установки исправлена 22.09: сервер живёт в
  Europe/Moscow, поэтому `4 7 * * *` отправлял в 07:04 МСК (до брифа) вместо
  оговорённых 10:04; строка переведена на `4 10 * * *` = 07:04 UTC. — `done`
- 338.3 Учёт: лог в `logs/mcp/` LiveHelperAgent, строка в
  `reference_vetmanager_mcp_prod` (откуда сообщение и как выключить); первое
  сообщение из группы — Владимиру. Закрытие этапа — после первого
  автоматического сообщения по крону, а не после ручного запуска. Лог
  `logs/mcp/2026-09-21-stage-338-daily-heartbeat.md`, память обновлена 21.09.
  Крон отработал сам 22.09 (`CRON[2806463]`, релей 204), доставку в группу
  Владимир подтвердил 21.09 — этап закрыт 22.09. — `done`

Что этап не делает: аварию в 14:00 узнаем в 10:04 назавтра — осознанно;
пятиминутные пороги остаются у `vps-monitor.sh`. Продуктовые алерты (доля
5xx апстрима, застрявшие Report AI, всплеск no_match) — отдельное решение,
если суточного ряда окажется мало.

## Этап 339. Путь подключения клиники не повторяет запрос при таймауте чтения — `done`

Источник: production-наблюдение супервизора 22.09.2026: шесть
`ReadTimeout` при проверке подключения без единого retry; позднее тот же
домен отвечал за 0.27 с. `_request_with_retry` повторяет connect timeout и
502/503/504, но немедленно завершает GET при read timeout и сетевой ошибке.

- 339.1 Идемпотентный GET повторяет read timeout и транспортные ошибки в
  пределах общего 45-секундного бюджета; TLS verification остаётся
  немедленным диагностическим отказом. — `done`
- 339.2 POST не повторяется после read timeout/транспортной ошибки;
  ConnectTimeout по-прежнему допускает повтор для любого метода. — `done`
- 339.3 Retry-логи и метрики остаются по попыткам; сторожа red/green, живая
  проверка подключения devtr6, полный Core Loop и CI. — `done`

## Этап 322. Массовая переоценка может записаться наполовину — `done`

Источник: супер-ревью 16.09.2026, F4, F5. Переоценка склада делает PUT и
проверочный GET построчно и при первом сбое бросает исключение, не сообщая,
какие строки уже записаны; повтор процентной переоценки накладывает
коэффициент второй раз на уже изменённые цены. Пагинация `_all_rows`
подменяет отсутствующий `totalCount` длиной собранного и после полной
первой страницы останавливается — часть товаров не переоценивается молча.

- 322.1 Отчёт о частичной записи: инструмент возвращает список записанных и
  незаписанных строк, повтор процентной переоценки по уже изменённым строкам
  запрещён или идемпотентен. — `done`
- 322.2 Пагинация переоценки без `totalCount` — fail-closed: явная ошибка
  или дочитывание до пустой страницы; красный сторож. — `done`

## Этап 323. Пагинация верит `totalCount`, которого может не быть — `done`

Источник: супер-ревью 16.09.2026, F16, F17, F18, F19. `paginate_all`
трактует отсутствующий `totalCount` как ноль и останавливается после первой
непустой страницы, а заниженному `totalCount` при полной странице верит.
Закрывающие записи по счёту склеивают по одной странице с каждой стороны и
режут глобальным offset/limit. Поиск пользователя по имени склеивает
независимо отсортированные страницы фамилий и имён и выдаёт длину слияния как
`totalCount`. `get_invoice_documents_by_period` не отличает HTTP 200 с
`success=false` от пустой страницы и возвращает `limited=false, totalCount=0`.

- 323.1 Общий helper пагинации: без `totalCount` — дочитывание до пустой
  страницы, заниженный `totalCount` при полной странице — продолжать;
  сторожа на оба случая. — `done`
- 323.2 Закрывающие записи и поиск пользователей: честный обход обеих сторон
  до конца или явный признак `limited`, `totalCount` не выдумывается. — `done`
- 323.3 `success=false` при HTTP 200 — ошибка приложения, а не пустая
  страница, во всех инструментах периода. — `done`

## Этап 324. Внешние вызовы без бюджета — `done`

Источник: супер-ревью 16.09.2026, F11, F21. Поиск клиента по имени делает
три upstream-запроса на каждый токен строки и собирает их все параллельно —
длинная строка превращается в десятки одновременных вызовов к клинике. Retry
GET разрешает три повтора, каждый `Retry-After` может усыпить на 300 секунд,
а суммарного лимита времени на логический запрос нет.

- 324.1 Поиск клиента: предел токенов и одновременных запросов, длинная
  строка — явная ошибка ввода или усечение с признаком. — `done`
- 324.2 Retry: общий бюджет времени на логический запрос поверх лимита
  попыток; `Retry-After` сверх бюджета — отказ, не сон. — `done`

## Этап 325. Отключённый OAuth-клиент продолжает работать — `done`

Источник: супер-ревью 16.09.2026, F14, F15, F9, F23. Runtime-аутентификация
OAuth проверяет токен, grant, аккаунт и подключение, но не требует, чтобы
`OAuthClient` из `grant.client_id` оставался активным — выключение клиента не
гасит выданные access-токены. OAuth-запросы не проходят per-token rate
limiter, который действует для service bearer. Инвариант «одно активное
подключение на аккаунт» держится на process-local asyncio-lock без уникального
индекса в схеме — два процесса создадут два активных. `MCP_PATH` и
`SITE_BASE_URL` нормализуются в трёх местах по-разному.

- 325.1 Активность OAuth-клиента — обязательное условие аутентификации и
  scope-peek; сторож на отключённый клиент. — `done`
- 325.2 OAuth-запросы расходуют тот же per-token лимит, что и bearer. — `done`
- 325.3 Частичный уникальный индекс на активное подключение аккаунта в
  схеме + миграция; гонка двух процессов воспроизводится красным тестом. — `done`
- 325.4 Одна функция нормализации `MCP_PATH`/`SITE_BASE_URL` для OAuth-метаданных,
  старта сервера и onboarding-страниц. — `done`

## Этап 326. Архив Roadmap сломал сторожа завершения этапа — `done`

Источник: супер-ревью 16.09.2026, F12, F13, F7, F8, F26, F27, F28, F29 —
хвосты этапа 319. `check_stage_completion.sh` ищет заголовок только в
`Roadmap.md`, и этап, перенесённый в архив шагом 13, на том же прогоне
считается `missing_roadmap_header`. У закрытого этапа 317 нет секции в
`AssumptionLog.md` — обязательное evidence отсутствует. Архиватор удаляет
блоки очереди по множеству имён: при дубле имени уезжает и активный этап;
запись двух файлов не атомарна. Порог `< max − 20` оставляет 21 номер, гейт
не ловит свежий закрытый этап в архиве. Записи 319 в Roadmap/PRD расходятся
с реализацией (порядок архива — по закрытию, не по номерам). Core Loop и
Cursor-правило не требуют запуска `archive_roadmap.py` перед гейтом; статус
`supervisor_pending` не знают checker и `CLAUDE.md`.

- 326.1 Секция «Этап 317» в `AssumptionLog.md` с evidence по фактическим
  коммитам и деплою. — `done`
- 326.2 `check_stage_completion.sh` и прочие потребители читают заголовок из
  обоих файлов. — `done`
- 326.3 Архиватор: удаление по номеру этапа, а не по имени; запись через
  временный файл и атомарную замену; сторожа на дубль имени и на обрыв
  между записями. — `done`
- 326.4 Окно ровно 20 номеров; гейт отклоняет закрытый этап из окна, попавший
  в архив; максимум считается по обоим файлам. — `done`
- 326.5 Текст 319 в Roadmap и PRD приведён к реализации (порядок закрытия,
  фактическое число исправленных заголовков). — `done`
- 326.6 Core Loop в `CLAUDE.md`, Cursor-правило и `review_workflow_check.sh`:
  шаг `archive_roadmap.py` перед структурным гейтом, `Roadmap-archive.md` в
  обязательных артефактах, `supervisor_pending` в словаре статусов. — `done`

## Этап 327. Отправка сообщений не видна в метриках инструментов — `done`

Источник: супер-ревью 16.09.2026, F25, F20. Три инструмента отправки
сообщений зовут `VetmanagerClient.post` напрямую, минуя `instrument_call`:
upstream-метрики есть, серии исхода и латентности инструмента — нет.
Относительные даты, документированные как «по локальному времени клиники»,
считаются от `date.today()` процесса — в мультитенантном сервисе это
день не той клиники на границе суток.

- 327.1 Инструменты отправки сообщений проходят через `instrument_call`;
  сторож на наличие серий. — `done`
- 327.2 Относительные даты привязаны к таймзоне клиники (уже есть кэш
  таймзон Report AI — переиспользовать). — `done`

## Этап 328. Свойства клиники не должны раскрывать секреты — `done`

Источник: `report_problem` #70 и указание Владимира от 16.09.2026.

- 328.1 Закрыть выдачу секретных `properties` двумя независимыми слоями:
  denylist по имени/заголовку и санитайзер значения; сохранить честную
  пагинацию. — `done`
- 328.2 Убрать `property_value` из фильтра и сортировки, закрыв оракул
  существования значения. — `done`
- 328.3 Проверить контракт на read-only стенде, добавить mock- и real-suite
  сторожа и пройти полный Core Loop. — `done`

## Этап 329. Санитайзер свойств узнаёт распространённые ключи без порчи тумблеров — `done`

Источник: follow-up пользователя 17.09.2026 к issue #70.

- 329.1 Расширить value-level распознавание реальных API-токенов и синхронизировать real-oracle с production matcher. — `done`
- 329.2 Уточнить русскую морфему «ключ», сохранив ключи и не маскируя тумблеры; сравнить denylist на dump ВМ. — `done`
- 329.3 Пройти red/green, оба suite, review, CI и evidence. — `done`

## Этап 330. Маска телефонов принимает клинические числа за номера — `done`

Источник: репорт #74 (16.09.2026, аккаунт #50): в медкарте кота почасовой
диурез («… 50 45 60 55 40 …» мл) и координаты HTML-графиков анализов
приходят агенту как `[redacted-phone]` — врач не может оценить
постобструктивный диурез против скорости инфузии. Причина по коду:
`depersonalization.py:147` `_PHONE_RE = \+?\d[\d\-\s().]{8,}\d` — «телефон»
это любые две цифры на расстоянии ≥8 символов из цифр, пробелов, точек,
дефисов и скобок. Таблица из пяти чисел, ряд координат, интервал доз —
всё телефон. Второй такой же regex в `error_tracking.py:112`.

Решение (не спеши обрезать — сначала прогнать существующий корпус тестов
телефонов 275/308 и убедиться, что ни один реальный формат не перестаёт
маскироваться):

- 330.1 Телефон — только реалистичная форма, а не «много цифр с
  разделителями»: (а) сплошной ряд 10–15 цифр с необязательным `+`;
  (б) группированный формат `+CC? (XXX) XXX-XX-XX` с разделителями
  пробел/дефис/точка и группами 3–5 / 3 / 2 / 2; (в) международный с
  обязательным `+` и 2–4 группами по 2–4 цифры. Границы `(?<!\d)`/`(?!\d)`.
  Пять чисел подряд, ряд `10.5 20.3 …`, «50 45 60 55 40» — не телефон.
  Красный тест — таблица диуреза и координаты графика из #74 (синтетика);
  сторож — корпус реальных форматов (`+7 (999) 123-45-67`, `8 999 123 45 67`,
  `89991234567`, `+380 44 123 45 67`, `999-123-45-67`) остаётся
  замаскированным. — `done`
- 330.2 Контекстный стоп-лист второго слоя: число рядом с единицей
  (`мл`, `ммоль/л`, `мг`, `кг`, `%`, `°C`, `мм рт.ст.`) или со временем
  `ЧЧ:ММ` — не телефон, даже если форма совпала. — `done` (время из стоп-листа исключено по PRD-review: утечка «тел. … 10:00-18:00»; `г`/`ед` тоже; единицы только вплотную)
- 330.3 Тот же матчер в `error_tracking.py` (один источник правды, не две
  копии). — `done`
- 330.4 Репорт #74 → известная проблема `fixed`, real-suite проверка на
  стенде: медкарта с числовой таблицей читается без плейсхолдеров. — `done`

## Этап 331. Описания `create_pet` и `create_medical_card` расходятся с контрактом ВМ — `done`

Источник: репорты #71 и #73 (16.09.2026, аккаунт #39); продолжение этапа
321 — те же три поля в него не попали.

- 331.1 `create_pet`: описание обещает `breed_id` необязательным («0 — если
  неизвестно»), апстрим отвечает 422 `breed_id is required`. Проверить в
  `vetmanager-extjs` (`Pet.php`, контроллер REST), обязателен ли `breed_id`
  безусловно или зависит от типа; по результату — либо обязательный параметр
  в схеме и описании, либо подстановка справочной «неизвестной» породы для
  вида. — `done`
- 331.2 `create_medical_card`: описание подсказывает названия типа приёма
  («Первичный приём»), а апстрим молча пишет `admission_type=0` при названии
  и принимает только код справочника; `update_medical_card` не даёт
  исправить. Разрешать название в код по `combo_manual_items`
  (`admission_type`) или отклонять с ошибкой; описание — «код справочника»;
  `admission_type` в `update_medical_card`. — `done`
- 331.3 `create_medical_card`: `clinic_id` описан как необязательный
  (`0 = default`), апстрим — «No Clinic selected». Обязательный, как в
  `create_admission` после 321. — `done`
- 331.4 Репорты #71/#73 → известные проблемы `fixed`; real-suite на стенде
  по обоим сценариям. — `done`

## Этап 332. База знаний молчит на главном отказе недели — экспорт отчёта — `done`

Источник: срез 17.09.2026: 60 из 62 недельных поисков известной проблемы
без совпадения — `start_report_export` / `get_report_export_download` у
одного аккаунта вечером 11.09; репорт #80 (401 на скачивании после
успешного `startReport`) по отпечатку подтянулся к KI-45 «отчёт не
экспортируется по REST», но правила совпадения на тексты этих отказов у
KI-45 нет — доставка 1 из 68.

Применено супервизором вручную 19.09.2026 (снимок прода: KI-45 без отпечатка,
#80 с отпечатком `hmac-sha256:…` и связан с KI-45 — скрипт трижды отказывал
из-за регулярки на голые 64 hex, см. этап 335): `promote 80` → **KI-55**
«Report export file download is unauthorized» (отпечаток #80, правила и плейбук
download-401), `set-match-rules 45` / `set-playbook 45` из фикстур
`artifacts/known-issues/stage-332/`; `match-effectiveness --days 30` — все
`workaround_available` matchable. Evidence — `stage-332/*-manual*` вне репо.

- 332.1 По следам `no_match` (этап 317) и текстам ошибок 401/403 экспорта
  написать правила совпадения для KI-45 (и отдельную запись для 401, если
  семантика другая: «файл создан, скачивание не авторизовано»); плейбук —
  не повторять `start_report_export` после отказа (этап 317), сообщить
  пользователю причину. — `done`
- 332.2 Сторож: воспроизведённый отказ экспорта на стенде находит известную
  проблему (`match-effectiveness` показывает доставку). — `done`

## Этап 333. Скрипт применения 332 не рассчитан на KI-45 без отпечатка (follow-up этапа 332) — `done`

- 333.1 PRD и сторожа для source fingerprint present/absent/mismatch. — `done`
- 333.2 Минимальная CLI/script правка, фикстуры и локальная live-проверка. — `done`
- 333.3 Полные suite/review/deploy/закрывающий CI. — `done`

## Этап 334. Миграция 332 при отсутствии отпечатков и у KI-45, и у репорта #80 (follow-up 333) — `done`

- 334.1 PRD и сторожа третьего состояния `null/null`. — `done`
- 334.2 Rule-only миграция, rollback/retry и локальная live-проверка. — `done`
- 334.3 Полные suite/review/deploy/закрывающий CI. — `done`

## Этап 335. Скрипт применения 332 отвергает реальный формат отпечатка — `done`

Источник: три отказа fail-closed 19.09.2026 (13:28, 15:43, 17:31). Реальный
`error_fingerprint_hash` имеет вид `hmac-sha256:<64 hex>`
(`agent_feedback_service.py`, `f"hmac-sha256:{digest}"`), а
`scripts/apply_stage332_known_issues_prod.sh` проверяет `^[[:xdigit:]]{64}$`
(строки ~70 и ~105) — отпечатки #80 и любых записей считаются «невалидными»;
тесты 333/334 использовали синтетические 64-hex строки и не ловили это.
Фактическое состояние прода перед миграцией: KI-45 `null`, #80 `hmac-sha256:…`,
связан с KI-45 — то есть «односторонний отпечаток», который 334 объявил
fail-closed. Миграция 332 уже применена вручную (KI-55); этап нужен, чтобы
скрипт и сторожа соответствовали реальному контракту на будущее.
Код `e56806f` доставлен: Tests `35468427661`, ShellCheck
`35468427617` и Deploy Prod `35468802910` завершились `success`.
Миграция 332 повторно не запускалась; production DB/`known_issues` не
изменялись.

- 335.1 Регулярки отпечатка в скрипте и тестах — на реальный формат
  `hmac-sha256:` (+ голые 64 hex для обратной совместимости, если встречаются
  в БД); фикстуры тестов — с префиксом. — `done`
- 335.2 Состояние «источник без отпечатка, репорт с отпечатком, связан» —
  поддерживаемое (`promote` без переноса), а не fail-closed; rollback для него;
  сторож красным. — `done`
- 335.3 Сторож на формат: тест, что regex скрипта принимает значение, которое
  выдаёт `agent_feedback_service` для реального инцидента. — `done`
